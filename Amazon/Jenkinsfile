pipeline {
    agent any
    environment {
        // Use PATH+EXTRA to append to PATH properly
        PATH = "/home/giri/girija_repo/project_repo/Amazon/Amazon/Amazon-Web/target"
    }
    stages {

        stage('pull') {
            steps {
                git branch: 'main', url: 'https://github.com/Girija-U/Amazon/Amazon-Jenkins.git'
            }
        }
        stage('compile') {
            steps {
                sh 'mvn compile'
            }
        }

        stage('build') {
            steps {
                 sh 'mvn clean install'
            }
        }

        
    }

  post{

  success{
     echo 'Build success'
  }
    
  failure{
       echo 'Failure in the build'
   }

  }


}
